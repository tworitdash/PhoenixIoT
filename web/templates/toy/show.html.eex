<h2>Show toy</h2>

<ul>

  <li>
    <strong>Body:</strong>
    <%= @toy.body %>
  </li>
  
  <ul id = "show-list" data-id = <%= @current_user.id %>>

  </ul>

  <%= @current_user.token %>
  <%= @current_user.username %>

  <li>
    <strong>Name:</strong>
    <%= @toy.name %>
  </li>

</ul>

<%= link "Edit", to: toy_path(@conn, :edit, @toy) %>
<%= link "Back", to: toy_path(@conn, :index) %>
